import Page from '../components/ContentPage';

export default function Returns() {
    return <Page title="Returns & Exchanges" content={<p>We offer a 7-day return policy for all unused items in their original packaging.</p>} />;
}
